# Base URL used to build the artifact URLs this service returns. Must be the
# address callers (e.g. ai-workforce's worker) can actually reach.
ARTIFACT_BASE_URL=http://localhost:8090

# If set, POST /v1/artifacts requires Authorization: Bearer <key>.
# Empty = no auth gate (local dev only).
ARTIFACT_API_KEY=
